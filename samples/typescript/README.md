# TypeScript Sample Issues

This sample intentionally includes:
- Missing type annotations and overuse of `any`
- `useEffect` without dependency array
- Event listener cleanup leak
- Missing async error handling
- `console.log` in production code
- Potential XSS via `dangerouslySetInnerHTML`
- Missing React list `key`
- Hardcoded token and unsafe `eval`
